# skill: federate/sheaf
**Signature** Site = schema category with covers; skill = matching family
`{sᵢ ∈ F(Uᵢ)}` ↦ glued global section `s ∈ F(U)` when compatible.
**Semantics** Federation IS sheaf gluing: local stalks (peer ontologies),
restriction maps (alignments), global knowledge = global section. Failure
is not error but **obstruction** — a cohomology class witnessing genuine
contextuality (Abramsky–Brandenburger), reported as such.
**Law** Uniqueness: compatible locals glue to at most one global (no
central authority needed — the Hanseatic theorem).
**Binding** Peer schema morphism `F` induces `Σ_F ⊣ Δ_F ⊣ Π_F` (Spivak):
migration, pullback, join — federation's entire query algebra is one
adjoint triple.
