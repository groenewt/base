# skill: provenance/semiring
**Signature** `annotate : Sk a b → Sk (a, K) (b, K)` for commutative
semiring `K`; join = `·`, union = `+`.
**Semantics** Green–Karvounarakis–Tannen: positive relational algebra over
any commutative semiring factors through **ℕ[X]** — the universal
provenance semiring. Bag, set, probabilistic, why-provenance = homomorphic
images of one polynomial annotation.
**Fibers** Boolean (reachability) | tropical min,+ (cheapest derivation) |
Viterbi max,× (best explanation) | ℕ[X] (full lineage).
**Law** Annotation is a semiring homomorphism; evaluate once in ℕ[X],
specialize everywhere.
**Binding** Every GraphAtlas answer carries its polynomial: audit is
substitution, trust is evaluation.
